import { SELECTOR_CONTENT, SELECTOR_NAV } from './constants';

export const contentElement = () => document.querySelector(SELECTOR_CONTENT);
export const navElement = () => document.querySelector(SELECTOR_NAV);
